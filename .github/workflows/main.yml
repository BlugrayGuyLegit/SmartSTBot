name: Run Python Bot

on:
  push:
    branches:
      - main
  schedule:
    - cron: '59 */5 * * *'  


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: purge cache
      run: pip cache purge

    - name: chatterbot
      run: pip show chatterbot
           pip show chatterbot-corpus
           pip install chatterbot
           pip install chatterbot-corpus
           pip install ChatterBot
           pip show chatterbot
           pip show chatterbot-corpus

    - name: site
      run: python -m site --user-site

    - name: update
      run: pip install --upgrade pip

    - name: Install dependencies
      run: pip install -r requirements.txt
           pip install setuptools
           pip install wheel
           pip install Cython
           pip install cymem
           pip install preshed 
           
    - name: Run bot
      run: python bot.py & python responses.py
      env:
        DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
        CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
